import { useState, useEffect, useCallback } from 'react'; interface Use{{pascalCase
  name
}}Return { // 定义你的返回值类型 } /** *
{{camelCase name}}
hook * 自定义 Hook 功能描述 */ export const use{{pascalCase name}}
= (): Use{{pascalCase name}}Return => { const [state, setState] =
useState(null); useEffect(() => { // 初始化逻辑 }, []); const functionName =
useCallback(() => { // 你的逻辑 }, []); return { // 返回值 }; }; export default
use{{pascalCase name}};