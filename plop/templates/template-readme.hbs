# {{name}} Template

{{description}}

## 特性

{{#each features}}
{{#if (eq this "typescript")}}
- ⚡️ TypeScript 支持
{{/if}}
{{#if (eq this "css-preprocessor")}}
- 🎨 CSS 预处理器支持
{{/if}}
{{#if (eq this "state-management")}}
- 🔄 状态管理
{{/if}}
{{#if (eq this "routing")}}
- 🛣️ 路由功能
{{/if}}
{{#if (eq this "testing")}}
- 🧪 测试框架
{{/if}}
{{#if (eq this "bundler")}}
- 📦 打包工具
{{/if}}
{{/each}}

## 快速开始

```bash
# 安装依赖
npm install

# 启动开发服务器
npm run dev

# 构建生产版本
npm run build
{{#if features.includes("testing")}}

# 运行测试
npm test
{{/if}}
```

## 项目结构

```
src/
├── components/  # 组件目录
├── utils/       # 工具函数
├── styles/      # 样式文件
└── main.js      # 入口文件
```

## 开发指南

1. 在 `src/components/` 目录下创建新组件
2. 在 `src/utils/` 目录下添加工具函数
3. 在 `src/styles/` 目录下管理样式文件

## 许可证

MIT