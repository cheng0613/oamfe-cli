{
  "name": "{{name}}-template",
  "version": "1.0.0",
  "description": "{{description}}",
  "main": "index.js",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"{{#if features.includes("testing")}},
    "test": "vitest"{{/if}}
  },
  "devDependencies": {
    "vite": "^5.0.8"{{#if features.includes("typescript")}},
    "typescript": "^5.2.2"{{/if}}{{#if features.includes("testing")}},
    "vitest": "^1.0.0"{{/if}}{{#if features.includes("css-preprocessor")}},
    "sass": "^1.69.0"{{/if}}
  }
}